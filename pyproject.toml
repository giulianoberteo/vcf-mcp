[project]
name = "vcf-mcp"
version = "0.4.0"
description = "MCP server for VMware Cloud Foundation Fleet Management, Operations, and SDDC Manager APIs"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.0.0",
    "httpx>=0.27.0",
]
