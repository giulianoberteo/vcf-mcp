[project]
name = "vcf-mcp"
version = "0.2.1"
description = "MCP server for VMware Cloud Foundation Fleet Management and Operations APIs"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.0.0",
    "httpx>=0.27.0",
]
